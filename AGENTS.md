# 校园闲置开发约定

## 工程铁规
1. 每个原子代码变更都必须有可回滚的 Git 快照：动手前确认并记录可恢复的基线，完成且验证通过后创建内容单一、说明清晰的 commit。不得覆盖历史、提交无关改动，或把未验证状态标记为完成。
2. 每次代码改动后必须同步更新受影响的测试，并实际运行相关测试直至通过；随后运行项目约定的完整验证。不得通过删除测试、降低断言或跳过失败来制造通过结果。若当前没有测试体系，先建立覆盖主流程的最小可运行测试入口。

## 项目验证命令
- 安装依赖：`npm ci`
- 相关测试：`npm test`
- 完整验证：`npm run check`（JavaScript 语法检查、小程序文件检查、API / 小程序逻辑测试）；界面变更另运行 `npm run test:e2e`，本机可设置 PLAYWRIGHT_CHANNEL=msedge。
- 本地演示：`npm run dev`，只监听 127.0.0.1，禁止部署演示登录到公网。

## 产品边界
依据 docs/product-design.md：单校、免费信息发布、线下联系交易，无商品订单、支付、抽佣或付费置顶。
所有秘密只从环境变量读取；生产环境关闭演示登录；用户只能编辑自己的商品；管理员审核后公开。
