# 盒闲验证记录

日期：2026-09-17。环境：Windows、Node.js 24.14.1、Playwright + 本机 Microsoft Edge。

## 本次 UI 接入已验证

- `npm run check`：JavaScript 语法、JSON 配置、小程序页面文件完整性检查通过；12 项 API / 数据库备份 / 小程序逻辑测试通过。
- `PLAYWRIGHT_CHANNEL=msedge npm run test:e2e`：10 项浏览器测试通过，其中业务前台5项、独立静态原型5项。
- 真实业务双用户闭环：照片上传 → 发布待审核 → 管理员审核 → 另一用户搜索 → 详情简介 → 收藏 → 登录查看联系方式 → 标记已售 → 收藏失效提示与取消。
- 新 UI：顶部就地展开并逐字请求真实商品、搜索结果打开详情、5秒轮播、五入口导航、“我的”子入口、失物/消息未开放提示。
- 照片：两次追加至6张，满额加号隐藏，删除后恢复；原型另覆盖超额选择、同一文件重选、取消收藏同步。
- 微信逻辑：即时搜索与过期响应丢弃、导航页面注册、满6张停止选图；已有发布参数、失败保留输入、上传中阻止提交和登录同意测试继续通过。
- 手机320 / 375 / 390 / 430px及桌面1440px无横向溢出；人工查看真实首页、我的、发布表单截图。
- 本次未更改后端鉴权、所有权、人工审核或数据库结构。原有越权、媒体权限、状态校验、金额精度及WAL备份测试均通过。

## 尚未验证

- 微信开发者工具 WXML/WXSS 编译、真机自定义tabBar、真实微信登录、照片授权、提审发布。逻辑测试不能替代微信编译或真机验收。
- Docker构建、阿里云ECS实机部署和大规模并发。
- 失物服务、站内聊天、交易反馈后台、支付、校园身份核验、OSS与自动审核均未实现。

本次未重新运行依赖漏洞审计，不沿用旧记录宣称当前安全审计通过。Git上传结果以实际push结果为准；上传代码不代表上线。
